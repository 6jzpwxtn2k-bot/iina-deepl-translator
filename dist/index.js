"use strict";

// index.js - per-video context
// Menu is registered in global.js; this file is intentionally minimal.
// IINA requires an entry point file — no duplicate menu registration here.

const { core } = iina;

// No additional setup needed per-video.
// Translation logic and menu are handled by global.js.
